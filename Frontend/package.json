{
  "dependencies": {
    "react-icons": "^5.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "proxy": "http://localhost:5000"
}
